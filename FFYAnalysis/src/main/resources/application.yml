server:
  port: 8989
  servlet:
    context-path: /ffy
spring:
  application:
    name: FFAnalisys
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ffy?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: admin
  thymeleaf:
    cache: false

mybatis:
  mapper-locations: classpath:/mapper/*.xml

log:
  runtime:
    level: DEBUG

logpath:
  runtime:
    path: ./log

